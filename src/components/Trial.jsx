import { Button } from '@mui/material'
import React from 'react'

const trial = () => {
  return (
    <div>
      <Button variant="contained" color="primary">Home Page</Button>
      <Button variant="contained" color="error">Gallery Page</Button>
      <Button variant="contained" color="success">Contact page</Button>
    </div>
  )
}

export default trial

